@extends('layout')
@section('content')<div class="card"><h1>{{ $item->exists ? 'Edit' : 'Tambah' }} Barang</h1><form method="POST" action="{{ $item->exists ? route('barang.update',$item) : route('barang.store') }}">@csrf @if($item->exists)@method('PUT')@endif<label>Kode Barang</label><input name="kode_barang" value="{{ old('kode_barang',$item->kode_barang) }}" required><label>Nama Barang</label><input name="nama_barang" value="{{ old('nama_barang',$item->nama_barang) }}" required><label>Kategori</label><select name="kategori_id" required><option value="">Pilih</option>@foreach($kategoris as $x)<option value="{{ $x->id }}" @selected(old('kategori_id',$item->kategori_id)==$x->id)>{{ $x->nama }}</option>@endforeach</select><label>Satuan</label><select name="satuan_id" required><option value="">Pilih</option>@foreach($satuans as $x)<option value="{{ $x->id }}" @selected(old('satuan_id',$item->satuan_id)==$x->id)>{{ $x->nama }}</option>@endforeach</select><label>Stok Awal</label><input type="number" min="0" name="stok" value="{{ old('stok',$item->stok ?? 0) }}" required><label>Stok Minimum</label><input type="number" min="0" name="stok_minimum" value="{{ old('stok_minimum',$item->stok_minimum ?? 0) }}" required><label>Keterangan</label><textarea name="keterangan">{{ old('keterangan',$item->keterangan) }}</textarea><button class="btn">Simpan</button></form></div>@endsection